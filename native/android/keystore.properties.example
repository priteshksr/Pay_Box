# Copy this file to android/keystore.properties and fill it in.
# Never commit the real keystore.properties — the .gitignore already
# excludes it.
#
# See NATIVE.md "Android keystore" for how to create the .jks.

storeFile=../../release.jks
storePassword=REPLACE_ME
keyAlias=paybox
keyPassword=REPLACE_ME

node_modules/
www/
dist/
build/

# Generated per-platform folders. These are committed in most Capacitor
# projects so native tweaks (Info.plist, AndroidManifest.xml) are
# versioned. Comment these two lines out once you're ready to commit
# them.
ios/App/App/public/
android/app/src/main/assets/public/

# Signing material (NEVER commit keystores or provisioning profiles)
*.keystore
*.jks
*.p8
*.p12
*.mobileprovision
google-services.json
GoogleService-Info.plist
keystore.properties

# Generated by Xcode / Android Studio
.DS_Store
.gradle/
.idea/
local.properties
*.xcuserstate

# Capacitor runtime logs
capacitor.log
